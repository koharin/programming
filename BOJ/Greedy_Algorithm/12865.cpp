#include <iostream>
#include <vector>

using namespace std;

int main(){
    cin.tie(NULL);
    ios::sync_with_stdio(false);
    unsigned int N,K;

    cin >> N >> K;
    vector<unsigned int> W(N,0);
    vector<unsigned int> V(N,0);

    for(int i=0; i<N; i++){
        unsigned int w,v;
        cin >> w >> v;
        W.push_back(w);
        V.push_back(v);
    }

    for()
}